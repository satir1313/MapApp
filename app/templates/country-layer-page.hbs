   {{#mapbox-gl class='map-container full-screen' initOptions=(hash pitch=10) as |map|}}
    {{#map.source options=(hash type='vector'
        url='mapbox://mapbox.mapbox-terrain-v2') as |source|}}
        {{source.layer layer=(hash id='mapbox-terrain' type='line' paint=(hash line-color='#ff69b4') layout=(hash visibility='visible' line-join='round') 
            source-layer='mapbox-terrain')}}
    {{/map.source}}
    {{/mapbox-gl}} 